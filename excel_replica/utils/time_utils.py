"""Datetime helpers for hourly timelines."""

import pandas as pd


def to_datetime(series: pd.Series) -> pd.Series:
    """Convert a series to pandas datetime."""
    raise NotImplementedError("Implement datetime conversion.")
